import topics from './topics';
import users from './users';
import fiches from './fiches'
import playlists from './playlists'

export { topics, users, fiches, playlists };

export default {
  topics,
  users,
  fiches,
  playlists
};
